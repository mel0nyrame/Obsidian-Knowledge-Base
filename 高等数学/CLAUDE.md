# Wiki Schema — 期末复习 Schema

> 这是 Claude 的行为配置文件（Schema）。它定义了这个知识库当前阶段的结构、约定和工作流。
> 每次会话开始时，Claude 应先阅读本文件，再开始工作。

---

## 📌 当前阶段：期末复习（写笔记模式）

**当前日期**：2026-07-05
**目标**：通过写笔记的方式复习高等数学期末考试内容
**模式**：用户写笔记 → Claude 协助修订、补充、答疑（**不主动维护 wiki**）

> 🔄 **模式变更（2026-07-05）**：从「Wiki 模式」切回「写笔记模式」。`wiki/` 目录已搭建好（来源摘要 + 概念页 + 索引），用户可随时浏览，需要改动时通过对话让 Claude 维护，但**不主动同步 wiki**。

### 🎯 考试导向原则（v1.6 — 基础保留 + 简洁 + 考试导向）

> **用户的目标是期末考试作出题目，不是深入研究/掌握理论。**
>
> **保留什么**（必须有的）：
> - **基础概念 / 定义**：一句话点到为止（如"既有大小又有方向的量称为向量"），不要砍
> - **关键运算性质 / 定律**：该有的一个都不能少（交换律、分配律、结合律等），不能为简洁而删
>
> **简化什么**（不要写太复杂）：
> - **不要**展开成教科书式的长篇定义 / 严格推导 / 完整证明
> - **不要**堆砌"显然易证"、"由定义可知"之类的填充
> - **点到为止**：能让用户明白意思即可，不必每一步都写
>
> **叠加什么**（在基础之上额外给）：
> - 凡是能用「看到 XX → 做 YY」格式表达的**判定规则 / 解题口诀 / 做题技巧**，**必须**写出来
> - 凡是考试常用的快速方法（判定口诀、记忆口诀、判别式、套用模板），**必须**给
>
> **判断标准**：在考场上看到这个知识点时，能不能立刻动笔？不能，就是笔记没写到位

### 复习模块（按你给出的顺序）

| # | 模块 | 备注 |
|---|------|------|
| 1 | 常微分方程 | ⚠️ **上学期内容，可能遗忘**，需要重点回顾 |
| 2 | 向量与空间解析几何 | 本学期 |
| 3 | 多元函数微分学 | 本学期 |
| 4 | 多元函数微分学的应用 | 本学期 |
| 5 | 多元函数积分学 I | 本学期 |
| 6 | 多元函数积分学 II | 本学期 |
| 7 | 无穷级数 | 本学期 |

### Claude 的角色

- **不主动维护 wiki**（只保留笔记整理规则 + v1.6 考试导向原则）
- **`wiki/` 目录可浏览**：用户随时可以看 wiki/ 下的页面，但**不主动同步更新**
- 当用户说"整理笔记""做一份完整版""修订补充"时 → 触发笔记整理规则（见下文）
- 当用户提出数学问题（"这个积分怎么做""这一步怎么来的"）→ 直接解答
- 当用户问"这块复习得怎么样了" → 通过 `raw/素材/` 下的笔记文件作答
- 当用户要求同步更新 wiki（如"把今天整理的同步到 wiki"） → 才更新 wiki

---

## 仓库结构

```
高等数学/
├── CLAUDE.md              ← 本文件：Schema 配置
├── karpathy的wiki方法论.md ← 参考文档（Wiki 模式备查）
├── raw/
│   └── 素材/              ← 用户的复习笔记（按章节命名）
└── wiki/                  ← 已搭建（用户浏览，按需更新；当前不主动维护）
    ├── index.md
    ├── log.md
    ├── 来源/
    ├── 概念/
    └── 对比/
```

### 当前各目录用途

| 目录 | 用途 | 谁写 |
|------|------|------|
| `raw/素材/` | 用户写原始笔记、Claude 协助修订生成的 `_完整.md` | 用户写初稿，Claude 协助修订 |
| `wiki/` | 已搭建好可浏览 | **不主动维护**；用户要求时才更新 |

---

## 📝 笔记整理规则（v1.0 — 2026-06-29）

> 适用于用户提供的随笔型学习笔记（如 `raw/素材/第X章.md`），生成 `_完整.md` 修订版。

### 触发条件

- 用户说"整理笔记""修订补充""做一份完整版"等
- 源文件包含 `// 开头的提示词`（给 Claude 的指令）
- 源文件存在明显错别字、漏字、语序混乱

### 工作流程

1. **通读原笔记**，标注：
   - 所有错别字（按上下文语义还原）
   - 所有 `//` 开头的提示词（给 Claude 的指令）
   - 缺漏的内容（只有标题/列表项但没正文）
2. **按 `//` 提示词执行规则**：
   - 提示词明确要求"扩展题型/补充例子" → 可以扩展
   - 提示词只要求"形式化准确、符号正确、补充完整" → 只补全已有的内容，不扩张新知识
3. **生成新文件**：
   - 命名格式：`原文件名_完整.md`（如 `1.集合.md` → `1.集合_完整.md`）
   - 放在**与原文件同一目录**（便于对照）
   - 删除所有 `//` 提示词
4. **核心原则**：
   - **🎯 考试导向**（最高优先级）：用户目标是期末考试作出题目，不是深入研究理论。
     - **保留**：基础概念 / 定义（一句话点到为止），关键运算性质 / 定律（该有的一个不能少）
     - **简化**：不展开成教科书式长篇定义 / 严格推导 / 完整证明，不堆砌"显然易证"之类填充
     - **叠加**：凡是能用「看到 XX → 做 YY」表达的判定规则 / 解题口诀 / 做题技巧，必须写出来；考试常用的快速方法（判定口诀、记忆口诀、判别式、套用模板）必须给
   - **跟随用户思路**：保留原文的结构、章节顺序、个人化表述
   - **不扩张内容**：除非 `//` 提示词明确允许，否则不引入新概念、新题型
   - **形式化优先**：符号、公式、定理名称必须准确
   - **错别字修订**：根据上下文推断原意，不臆造
   - **纯笔记输出**：生成的 `_完整.md` 只包含笔记本身（标题 + 正文），**不写任何顶部/底部的修订说明、生成元注释、操作记录**。修订过程不污染笔记，过程信息只出现在对话里

### 默认假设

- 当用户说"不要扩张内容"时，严格遵循——只补全已写出的空位
- 当 `//` 提示词允许扩展时（如"适度扩展题型"），可以加少量例题，但不应改变原结构

### 输出检查清单

- [ ] 所有 `//` 提示词已删除
- [ ] 错别字已修订（语义通顺）
- [ ] 缺漏内容已补全（仅限原结构内的）
- [ ] 数学符号准确（$\in$、$\subseteq$、$\cup$、$\cap$ 等）
- [ ] 章节顺序与原笔记一致
- [ ] 未引入原笔记没有的新概念
- [ ] 笔记顶部/底部无任何修订说明、生成元注释、操作记录等非笔记内容
- [ ] **🎯 考试导向**：
  - 关键判定规则都用「看到 XX → 做 YY」/口诀/公式给出
  - 基础概念 / 定义 保留（一句话点到为止即可）
  - 关键运算性质 / 定律 没被砍掉
  - 没有教科书式长篇定义 / 严格推导 / 完整证明占据正文位置

---

## 🗂️ Wiki Schema 规则（**已暂停 / 按需恢复**）

> Wiki 模式 v2.0 已于 2026-07-05 短暂启用并搭建了基础（来源摘要 6 个、概念页 23 个、index、log）。当前模式切回"写笔记模式"，**不主动维护 wiki**，仅在用户明确要求时同步更新。

### 启用过的结构（已存在）

```
高等数学/
└── wiki/                      ← 已搭建，可浏览
    ├── index.md               ← 内容索引
    ├── log.md                 ← 操作日志（最后一条：2026-07-05 创建 23 个概念页）
    ├── 来源/                  ← 6 个章节来源摘要
    ├── 概念/                  ← 23 个核心概念页
    │   ├── ODE速通-4核心题型.md
    │   ├── 考前速记卡.md
    │   └── ...（按章节大粒度的概念页）
    └── 对比/                  ← 空
```

### 何时恢复 Wiki 主动维护

当用户说以下任一时**主动**启用 Wiki 维护工作流：
- "启用 wiki""开 wiki""维护知识库""同步到 wiki"
- "复习完一轮，沉淀成知识库"

### Wiki 维护工作流（暂停，恢复时启用）

#### 1. Ingest（摄取新原始资料）

当用户在 `raw/素材/` 写入新原始笔记后：

1. **通读原始笔记** + 已有的 `_完整.md`
2. **创建/更新来源摘要页**：`wiki/来源/YYYY-MM-DD-<章节名>.md`
3. **创建/更新概念页**：`wiki/概念/<概念名>.md`
4. **更新 `wiki/index.md`**
5. **追加 `wiki/log.md`**：`## [YYYY-MM-DD] 操作类型 | 标题`

#### 2. Query

读 `wiki/index.md` 找相关页面 → 深入读相关页面 → 综合回答

#### 3. Lint

查孤儿页面、过时信息、缺交叉引用、矛盾

### Wiki 页面约定（恢复时使用）

所有 wiki 页面都应有 YAML frontmatter：

```yaml
---
type: source | concept | comparison
tags: [tag1, tag2]
sources: [来源文件名]
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

### 索引与日志格式

- `wiki/index.md` 格式：表格列出所有页面，按类型分类
- `wiki/log.md` 格式：`## [YYYY-MM-DD] 操作类型 | 标题`

---

## 🚀 启动复习的快速参考

```bash
# 看现有笔记
ls raw/素材/

# 统计笔记数量
find raw/素材 -name "*.md" | wc -l

# 查某个模块的笔记
ls raw/素材/ | grep -i "常微"   # 或 ODE、向量、多元、级数 等
```

---

## 💡 给 Claude 的提示

0. **🎯 考试导向**（最重要，置顶）：用户的目标是**在期末考试中作出题目**，不是深入研究理论。
   - **保留**：基础概念 / 定义（一句话点到为止）、关键运算性质 / 定律（一个不能少）
   - **简化**：不展开成教科书式长篇定义 / 严格推导 / 完整证明
   - **叠加**：凡是考试常用的**判定口诀 / 做题技巧 / 套用模板 / 快速判别式**，**必须**写出来
   - **判断标准**：在考场上看到这个知识点时，能不能立刻动笔？不能，就说明笔记没写到位
   - 用户问「这个怎么求」「怎么判定」「怎么记」时，默认给**最快最直接的方法**，但不砍掉必要的基础解释
1. **不要主动创建 wiki**：除非用户明确说"建知识库""启动 wiki"，否则只在 `raw/素材/` 下维护笔记
2. **数学符号优先 LaTeX**：$$\int f(x)\,dx$$、$\nabla$、$\partial$、$\sum_{n=1}^{\infty}$ 等
3. **常微分方程模块要更耐心**：用户明确说可能遗忘，遇到基本概念（齐次、线性、特解、通解）可适当多解释
4. **保留用户原始笔迹**：修订版只改正错别字、补全空缺，不替换用户的表述风格
5. **每个模块可以单独成文件**：例如 `raw/素材/1.常微分方程.md`、`raw/素材/2.向量与空间解析几何.md` ……

---

*Schema 版本：v2.1 — 2026-07-05*（**写笔记模式 v1.6 + Wiki 暂停**——搭好 wiki 框架（6 章节来源摘要 + 23 概念页 + 1 考前速记卡 + 1 ODE 速通）后切回写笔记模式；保留 v1.6 考试导向原则和笔记整理规则；**wiki 不主动维护**，仅在用户明确要求时同步）