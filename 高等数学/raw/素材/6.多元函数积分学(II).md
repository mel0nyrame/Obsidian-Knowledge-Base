# 6.多元函数积分学(II)

// 给claude的提示词：这里我全都没学过，你要把我当一点都不懂的人来讲

## 第一节 对坐标的曲线积分的概念与性质

### 一、引例--变力沿曲线所做的功

### 二、对坐标的曲线积分的概念

对坐标x的曲线积分：

对坐标y的曲线积分：

备急函数：

积分互段：

对坐标的曲线积分：

第二类曲线积分：

应用上经常出现的是：∫LP(x,y)dx+Q(x,y)dy

对于空间上的：

### 三、对坐标的曲线积分的性质

1. 把邮箱光滑曲线户l分成l1和l2，则：

2. 设l是邮箱光滑曲线户，-l是l的方向曲线户，则有：

## 第二节 对坐标的曲线积分的计算

// 给claude的提示词：这里公式太抽象了，需要例题，例题的讲解需要一步步来，把我当成什么都不会的来讲，什么时候对x对y定积分，要讲的非常清楚

设函数P(x,y),Q(x,y)在邮箱光滑曲线户l上有定义且连续，l的参数方程为 x=(t) y= (t)，当参数t单调地有阿尔法编导被他，点m(x,y)从l的起点a沿着l运动到终点b。若(t) (t)在以阿尔法，被他为断电的闭区间上有连续函数，且 '^2(t) + '^2(t) != 0,则曲线积分∫LP(x,y)dx+Q(x,y)dy存在，且∫LP(x,y)dx+Q(x,y)dy = 

对于三元也是同理：

题型：计算曲线积分，定义域是一段弧/圆、圆弧，折线，三元情况下：曲线，从某点到某点的线段

## 第三节 曲线积分与路径无关的条件

### 一、格林公式

// 给claude的提示词：这一节需要做题技巧

单连通区域：

多连通区域：

// 给claude的提示词：这个公式需要解释一下是什么意思，要解释得明明白白清清楚楚

设有界闭区域d有分段光滑曲线户l所未曾。弱函数p=p(x,y),Q=Q(x,y)在d上具有连续偏导数，则有： 其中l是d的真相边界公司，该公司也称为格林公式

// 给claude的提示词：这里的例题也要每一步都讲的很清楚

题型：曲线积分直接套格林公式，计算二重积分定义域区域是闭合的，做辅助线段构造格林公式

### 二、平面上曲线积分与路径无关的条件

// 给claude的提示词：这里每一个定理都需要解释，然后需要一道简单一道复杂的例题，需要讲解得非常清晰，这里也需要做题技巧

**一定要记住：**

设函数 \(P=P(x,y),Q=Q(x,y)\) 在单连通区域 D 内具有连续偏导数，则下列条件相互等价：(1) 沿 D 中任一分段光滑的有向闭曲线 L，有
\(\oint_L P\mathrm{d}x + Q\mathrm{d}y = 0;\)(2) 对于 D 中任一分段光滑的有向曲线弧 L，曲线积分 \(\int_L P\mathrm{d}x + Q\mathrm{d}y\) 与路径无关，只与 L 的起点和终点有关；(3) \(P\mathrm{d}x + Q\mathrm{d}y\) 是 D 内某个函数 u 的全微分，即在 D 内存在函数 \(u(x,y)\)，使得
\(\mathrm{d}u = P\mathrm{d}x + Q\mathrm{d}y;\)(4) 在 D 内每一点处有 \(\dfrac{\partial P}{\partial y} = \dfrac{\partial Q}{\partial x}\)。

题型：证明曲线积分与路径无关，并计算值

### 三、全微分方程

P(x,y)dx+Q(x,y)dy=0 存在函数u(x,y)，使得du=P(x,y)dx+Q(x,y)dy，这个公式称为全微分方程

全微分方程的充要条件：

通解：

题型：求解微分方程，求方程通解

积分因子：

题型：利用积分因子解题

## 第四节 对坐标的曲面积分的概念

### 一、有向曲面的概念

双侧曲面：

单侧曲面：

上测：

下册：

外侧：

内侧

有向曲面：

### 二、引例--流向曲面一侧的流量

### 三、对坐标的曲面积分的概念

对坐标x，y的曲面积分：

备急函数：

积分曲面：

对坐标y，z的曲面积分：

对坐标z，x的曲面积分：

第二类曲面积分

性质1:如果把邮箱光滑曲面分成12，则：

性质2:设 是邮箱光滑曲面，是 的房车面，则

## 第五节 对坐标的曲面积分的计算

对坐标的曲面积分华为二重积分的公式：

题型：计算曲面积分的不同侧

## 第六节 高斯公式和斯托克斯公式

### 一、高斯公式

设空间有节闭区间是有分片光滑的比曲面所未曾的，函数p=p(x,y,z)，q=q(x,y,z),r=r(x,y,z)在 及 上具有连续偏导数，则有 这里 是 的整个便捷曲面的外侧，这个公式称为高斯公式

题型：利用高斯公式计算曲面积分，计算曲面积分，其中的定义域为曲面的某一侧

### 二、斯托克斯公式

// 给Claude的提示词：这里你自己补充，需要有行列式的表达形式，还有例题，以及上面右手法则那些

## 第七节 两类曲线积分、两类曲面积分之间的关系

### 一、两类曲线积分之间的关系

平面有向曲线户l上的两类曲线积分之间有如下联系：∫LPdx+Qdy=∫L(Pcosa+Qcosb)ds

空间上：∫LPdx+Qdy+Rdz=∫L(Pcosa+Qcosb+Rcosc)ds

### 二、两类曲面积分之间的联系

联系：

例题：

### 三、高斯公式、斯托克斯公式的另一种表示

设空间有界闭区域\(\Omega\)是由分片光滑的闭曲面\(\Sigma\)所围成的，函数\(P=P(x,y,z),Q=Q(x,y,z),R=R(x,y,z)\)在\(\Omega\)上具有连续偏导数，则有
\(\iiint_{\Omega} \left( \frac{\partial P}{\partial x} + \frac{\partial Q}{\partial y} + \frac{\partial R}{\partial z} \right) \mathrm{d}v = \oiint_{\Sigma} \left( P\cos\alpha + Q\cos\beta + R\cos\gamma \right) \mathrm{d}S, \tag{11-7-7}\)这里\(\Sigma\)是\(\Omega\)的整个边界曲面的外侧，\(\cos\alpha,\cos\beta,\cos\gamma\)是\(\Sigma\)上点\((x,y,z)\)处的法向量的方向余弦

题目：计算曲面积分的不同侧